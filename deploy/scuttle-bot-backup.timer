[Unit]
Description=Run the Scuttle Bot SQLite backup daily

[Timer]
OnCalendar=*-*-* 09:00:00
# Run on next boot if the machine was off at the scheduled time.
Persistent=true

[Install]
WantedBy=timers.target
